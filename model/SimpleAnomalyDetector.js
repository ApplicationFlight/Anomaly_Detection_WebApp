function get_anomalies(train_file, anomaly_file, algorithm) {
  console.log('we are in anomalies')
  // getting anomalies detections
  for (var prop in train_file) {
      if (Object.prototype.hasOwnProperty.call(train_file, prop)) {
        
      }
  }
  return 'done'
}

module.exports.get_anomalies = get_anomalies
